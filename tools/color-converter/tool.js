const input=document.getElementById('input'),output=document.getElementById('output'),message=document.getElementById('message'),preview=document.getElementById('preview');
function convert(){let v=input.value.trim(),r,g,b;if(/^#?[0-9a-f]{6}$/i.test(v)){v=v.replace('#','');r=parseInt(v.slice(0,2),16);g=parseInt(v.slice(2,4),16);b=parseInt(v.slice(4),16)}else{const m=v.match(/^rgb\s*\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*\)$/i);if(!m){message.className='message error';message.textContent='请输入 6 位 HEX 或 rgb(r, g, b)';return}[r,g,b]=m.slice(1).map(Number);if([r,g,b].some(x=>x>255)){message.className='message error';message.textContent='RGB 数值应为 0–255';return}}const hex='#'+[r,g,b].map(x=>x.toString(16).padStart(2,'0')).join('').toUpperCase();let rr=r/255,gg=g/255,bb=b/255,max=Math.max(rr,gg,bb),min=Math.min(rr,gg,bb),h=0,s=0,l=(max+min)/2,d=max-min;if(d){s=d/(1-Math.abs(2*l-1));h=max===rr?60*((gg-bb)/d%6):max===gg?60*((bb-rr)/d+2):60*((rr-gg)/d+4)}if(h<0)h+=360;output.textContent=`HEX: ${hex}\nRGB: rgb(${r}, ${g}, ${b})\nHSL: hsl(${Math.round(h)}, ${Math.round(s*100)}%, ${Math.round(l*100)}%)`;preview.style.background=hex;message.className='message';message.textContent='转换完成'}
document.getElementById('convert').onclick=convert;document.getElementById('copy').onclick=async()=>{if(output.textContent==='—')return;await navigator.clipboard.writeText(output.textContent);message.textContent='已复制'};input.addEventListener('input',convert);convert();