const input=document.getElementById('input'),output=document.getElementById('output'),message=document.getElementById('message');generate.onclick=async()=>{const bytes=new TextEncoder().encode(input.value),hash=await crypto.subtle.digest(document.getElementById('algorithm').value,bytes);output.textContent=[...new Uint8Array(hash)].map(x=>x.toString(16).padStart(2,'0')).join('');message.textContent='生成完成'};copy.onclick=async()=>{if(output.textContent==='—')return;await navigator.clipboard.writeText(output.textContent);message.textContent='已复制'};
