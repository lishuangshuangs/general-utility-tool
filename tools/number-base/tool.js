const input=document.getElementById('input'),from=document.getElementById('from'),output=document.getElementById('output'),message=document.getElementById('message');function run(){const n=parseInt(input.value.trim(),+from.value);if(!Number.isSafeInteger(n)){message.className='message error';message.textContent='请输入当前进制下的安全整数';return}output.textContent=`二进制：${n.toString(2)}\n八进制：${n.toString(8)}\n十进制：${n}\n十六进制：${n.toString(16).toUpperCase()}`;message.className='message';message.textContent='转换完成'}document.getElementById('convert').onclick=run;[input,from].forEach(x=>x.addEventListener('input',run));run();