const count=document.getElementById('count'),output=document.getElementById('output'),message=document.getElementById('message');function uuid(){if(crypto.randomUUID)return crypto.randomUUID();const b=crypto.getRandomValues(new Uint8Array(16));b[6]=b[6]&15|64;b[8]=b[8]&63|128;const h=[...b].map(x=>x.toString(16).padStart(2,'0')).join('');return`${h.slice(0,8)}-${h.slice(8,12)}-${h.slice(12,16)}-${h.slice(16,20)}-${h.slice(20)}`}function generate(){const n=Math.max(1,Math.min(50,+count.value||1));count.value=n;output.value=Array.from({length:n},uuid).join('\n');message.textContent=`已生成 ${n} 个 UUID`}generate.onclick=generate;copy.onclick=async()=>{await navigator.clipboard.writeText(output.value);message.textContent='已复制'};generate();
