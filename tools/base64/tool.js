const source=document.getElementById('source'),result=document.getElementById('result'),message=document.getElementById('message');function bytesToBase64(bytes){let s='';for(let i=0;i<bytes.length;i+=8192)s+=String.fromCharCode(...bytes.subarray(i,i+8192));return btoa(s)}encode.onclick=()=>{result.value=bytesToBase64(new TextEncoder().encode(source.value));message.textContent='编码完成'};decode.onclick=()=>{try{const binary=atob(result.value.replace(/\s/g,'')),bytes=Uint8Array.from(binary,c=>c.charCodeAt(0));source.value=new TextDecoder('utf-8',{fatal:true}).decode(bytes);message.className='message';message.textContent='解码完成'}catch{message.className='message error';message.textContent='Base64 内容无效或不是 UTF-8 文本'}};swap.onclick=()=>{[source.value,result.value]=[result.value,source.value]};clear.onclick=()=>{source.value=result.value='';message.textContent=''};
