const input=document.getElementById('input'),output=document.getElementById('output'),message=document.getElementById('message');function run(){try{const u=new URL(input.value.trim());output.textContent=JSON.stringify({协议:u.protocol,主机名:u.hostname,端口:u.port||'默认',路径:u.pathname,查询参数:Object.fromEntries(u.searchParams),锚点:u.hash||'—',来源:u.origin},null,2);message.className='message';message.textContent='解析完成'}catch{message.className='message error';message.textContent='请输入包含协议的完整网址'}}document.getElementById('parse').onclick=run;input.addEventListener('input',run);run();